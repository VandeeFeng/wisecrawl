{
  "name": "wisecrawl-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Hot news aggregation frontend interface",
  "scripts": {
    "predev": "mkdir -p src/data && LATEST_FILE=$(ls -t ../data/processed_output | head -n1) && cp ../data/processed_output/$LATEST_FILE src/data/$LATEST_FILE",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "vue": "^3.5.13",
    "@webtui/css": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.2",
    "vite": "^6.3.0"
  }
}
